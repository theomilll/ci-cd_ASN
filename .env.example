# Copy to .env and fill with the credentials for account 325583868777.
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
# Region that hosts the EKS cluster and pipeline infrastructure.
AWS_DEFAULT_REGION=eu-central-1
# Optional profile name (used if you run `aws configure --profile`)
AWS_PROFILE=
